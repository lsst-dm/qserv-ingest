[pytest]
log_cli = true
log_cli_level = DEBUG
log_format = %(asctime)s %(levelname)s %(message)s
log_date_format = %Y-%m-%d %H:%M:%S

markers =
    scale: scale tests (deselect with '-m "not scale"')
    dev: used to run only a set of tests during development

addopts =
    -m 'not scale'
