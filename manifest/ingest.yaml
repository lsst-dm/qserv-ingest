apiVersion: v1
kind: Pod
metadata:
  name: qserv-ingest
  labels:
    app: qserv
    instance: qserv
    run: qserv-ingest
    tier: ingest
spec:
  containers:
  - command:
    - sleep
    - "3600"
    image: qserv/qserv-ingest:c009718 
    imagePullPolicy: Always
    name: qserv-ingest
  
